# codex汉化增强plus版远程引用与后台清单

本文只统计本项目做的插件、运行时、安装器、服务与模型桥接相关逻辑，不统计 Codex 桌面应用本体自己的联网行为。

扫描时间：2026-06-13  
工作区：`e:\git\codex+`

## 结论

- 有后台：Windows 上安装了 `codex-plusplus-watcher` 服务，默认自动启动，每 5 分钟检查一次，负责自更新检查和重新修补 Codex 桌面注入层。
- 有远程 JSON：插件商店默认读取 `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/index.json`，索引和已审核源码包已镜像到 `https://github.com/chengyou888/-`。
- 有热更新/自更新：watcher 会调用 `update --watcher --quiet --no-repair`，通过 GitHub Releases 判断是否下载新版本；随后调用 `repair --watcher --quiet` 确保插件仍被加载。
- 当前公共热更新包：`https://github.com/chengyou888/-/releases/tag/v1.0.1`，release tag 指向 `codex-plusplus-source` 分支，源码包约 5.9 MB；npm 离线依赖仍放在公共仓库 `main/npm-tarballs`。
- 有第三方模型远程 API：DeepSeek、智谱 GLM、阿里千问/百炼接入会访问各自 API 域名。
- 有本地后台端口：模型桥接默认监听 `127.0.0.1:17661`；可选 Browser UI 监听 `127.0.0.1:8765`；可选远程调试监听 `127.0.0.1:9222`。

## 本地后台进程与端口

| 名称 | 类型 | 默认状态 | 地址/周期 | 作用 | 代码位置 |
|---|---|---:|---|---|---|
| `codex-plusplus-watcher` | Windows 服务 | 默认安装并自动启动 | 每 300 秒 | 后台运行 `update --watcher --quiet --no-repair`，然后运行 `repair --watcher --quiet` | `packages/installer/src/watcher.ts`，`packages/installer/assets/windows-service-host/main.go` |
| 模型桥接服务 | 本地 HTTP 服务 | 选择第三方模型后启动 | `http://127.0.0.1:17661/v1` | 给桌面 Codex 提供 `/models` 和 `/responses`，再转发到 DeepSeek/智谱/千问 | `packages/runtime/src/main.ts` |
| Browser UI 服务 | 本地 HTTP/WebSocket 服务 | 默认关闭 | `http://127.0.0.1:8765/` | 调试/浏览器 UI，需 `CODEXPP_BROWSER_UI=1`；当前命令实现是 macOS only | `packages/runtime/src/browser-ui.ts`，`packages/installer/src/commands/browser-ui.ts` |
| CDP 远程调试 | 本地调试端口 | 默认关闭 | `http://127.0.0.1:9222/json` | 需 `CODEXPP_REMOTE_DEBUG=1`；用于列出/调试 Chromium 目标 | `packages/runtime/src/main.ts`，`packages/runtime/src/codex-runtime-probe.ts` |
| tweaks 文件监听 | 本地文件 watcher | 插件启用时启动 | 本地 `tweaks` 目录 | 本地插件文件变化后热重载，不联网 | `packages/runtime/src/main.ts` |

### watcher 细节

- 服务名：`codex-plusplus-watcher`
- 显示名：`Codex++ Watcher Service`
- 触发周期：`WATCHER_INTERVAL_SECONDS = 5 * 60`
- 服务进程会隐藏控制台窗口：`SysProcAttr.HideWindow = true`
- 如果 `config.json` 里的 `codexPlusPlus.enabled === false`，服务跳过 update 和 repair。
- 如果 `autoUpdate === false`，`self-update` 在 watcher 模式下会跳过下载更新，但仍可执行 repair。

## 远程更新与热更新地址

| 功能 | 默认地址 | 触发时机 | 是否可配置 | 代码位置 |
|---|---|---|---|---|
| Codex++ release 检查 | `https://api.github.com/repos/chengyou888/-/releases/latest` | 设置页/侧边栏检查；watcher 自更新检查 | 可通过配置 `updateRepo` 或环境变量 `CODEX_PLUSPLUS_REPO` 改仓库 | `packages/runtime/src/main.ts`，`packages/installer/src/commands/self-update.ts` |
| Codex++ prerelease 检查 | `https://api.github.com/repos/{repo}/releases?per_page=20` | 发布通道选 `prerelease` 时 | 可配置 `updateRepo` | 同上 |
| Codex++ 源码下载 | `https://codeload.github.com/{repo}/tar.gz/{ref}`，当前稳定版为 `https://codeload.github.com/chengyou888/-/tar.gz/v1.0.1` | 执行自更新时 | 可通过 `CODEX_PLUSPLUS_REPO`、`CODEX_PLUSPLUS_REF`、配置 `updateRepo/updateRef` 改 | `packages/installer/src/commands/self-update.ts` |
| Windows 安装脚本下载 | `https://codeload.github.com/{repo}/zip/{ref}` | 运行 `install.ps1` | 可通过 `CODEX_PLUSPLUS_REPO`、`CODEX_PLUSPLUS_REF` 改 | `install.ps1` |
| Unix 安装脚本下载 | `https://codeload.github.com/{repo}/tar.gz/{ref}` | 运行 `install.sh` | 可通过 `CODEX_PLUSPLUS_REPO`、`CODEX_PLUSPLUS_REF` 改 | `install.sh` |
| update.ps1 拉安装器 | `https://raw.githubusercontent.com/b-nnett/codex-plusplus/main/install.ps1` | 本地 CLI 不存在时运行 `update.ps1` fallback | 固定写死 | `update.ps1` |
| update.sh 拉安装器 | `https://raw.githubusercontent.com/b-nnett/codex-plusplus/main/install.sh` | 本地 CLI 不存在时运行 `update.sh` fallback | 固定写死 | `update.sh` |

### 更新频率

- 运行时设置页的 Codex++ release 检查缓存：24 小时。
- watcher 自更新检查缓存：1 小时。
- watcher 服务循环：5 分钟一次，但不代表每 5 分钟都会下载；下载前会先检查缓存和版本。
- 自更新下载的是 GitHub 源码包，不是 EXE/DLL 差分包；下载后本机执行 `npm install/ci`、`npm run build`，再执行 `repair` 把运行时重新注入桌面 Codex。

## 插件商店与远程 JSON

| 功能 | 地址 | 触发时机 | 是否可配置 | 代码位置 |
|---|---|---|---|---|
| 插件商店索引 JSON | `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/index.json` | 打开/刷新 Tweak Store | 可通过 `CODEX_PLUSPLUS_STORE_INDEX_URL` 环境变量覆盖 | `packages/runtime/src/tweak-store.ts`，`packages/runtime/src/main.ts` |
| 安装商店插件源码包 | 优先使用索引里的 `archiveUrl`，当前镜像为 `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/archives/...`；没有 `archiveUrl` 时才回退到 `https://codeload.github.com/{entry.repo}/tar.gz/{entry.approvedCommitSha}` | 用户点击安装商店插件 | 由远程 store index 决定，仍要求 pin 到完整 commit SHA 并校验 manifest id/repo/version | `packages/runtime/src/tweak-store.ts`，`packages/runtime/src/main.ts` |
| 插件图标/资源预览 | 当前镜像为 `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/icons/...`；没有镜像图标时才使用 manifest 里的 `https://...` 或相对路径 | 商店卡片显示图标时 | 由 store entry/manifest 决定 | `packages/runtime/src/preload/settings-injector.ts` |
| Ai Open Tool 支持入口 | `https://aiopentool.com/` | 设置页维护区域点击反馈/支持时 | 固定地址 | `packages/runtime/src/preload/settings-injector.ts` |
| 历史审核准备接口：读取仓库信息 | `https://api.github.com/repos/{repo}` | 当前界面入口已隐藏；仅保留底层 IPC 兼容旧流程 | 用户输入 repo | `packages/runtime/src/main.ts` |
| 历史审核准备接口：读取 commit | `https://api.github.com/repos/{repo}/commits/{defaultBranch}` | 当前界面入口已隐藏；仅保留底层 IPC 兼容旧流程 | 用户输入 repo | `packages/runtime/src/main.ts` |
| 历史审核准备接口：读取 manifest | `https://raw.githubusercontent.com/{repo}/{commitSha}/manifest.json` | 当前界面入口已隐藏；仅保留底层 IPC 兼容旧流程 | 用户输入 repo | `packages/runtime/src/main.ts` |

### 插件商店风险点

- 商店索引是远程 JSON，可以在不更新本地程序的情况下变化；当前默认源已经换成 `chengyou888/-` 里的镜像索引。
- 实际安装源码时优先使用镜像索引里的 `archiveUrl`，没有镜像地址时才使用 `approvedCommitSha` 拼接 codeload 地址；完整 SHA 仍用于降低“分支漂移”风险。
- 已安装 tweak 的 release 检查会访问对应 `manifest.githubRepo` 的 GitHub Releases；代码检查更新状态，但不会仅因为检查到新版就静默替换 tweak 目录。

## 第三方模型 API

这些不是 Codex++ 自更新，是我们后来加的模型接入功能。只有用户配置 API Key、测试、刷新模型、或切换到对应模型桥接时才会访问。

| 供应商 | 默认 Base URL | 实际拼接接口 | 用途 | 代码位置 |
|---|---|---|---|---|
| DeepSeek | `https://api.deepseek.com` | `/models`，`/chat/completions` | 枚举模型、测试、主聊天桥接 | `packages/runtime/src/main.ts`，`packages/runtime/src/preload/settings-injector.ts` |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | `/models`，`/chat/completions` | 枚举模型、测试、主聊天桥接 | 同上 |
| 阿里千问 OpenAI 兼容模式 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `/models`，`/chat/completions` | 枚举模型、测试、主聊天桥接 | 同上 |
| 阿里百炼智能体应用模式 | `https://dashscope.aliyuncs.com/api/v1` | `/apps/{APP_ID}/completion` | 测试百炼智能体应用，不作为 Codex 主聊天模型 provider | 同上 |
| 阿里可部署模型 fallback | 根据 Base URL origin 拼 `https://dashscope.aliyuncs.com/api/v1/deployments/models?page_no=1&page_size=100&version=v1.0&model_source=base` | 模型列表 fallback | 千问 `/models` 失败后尝试 | `packages/runtime/src/main.ts` |

### 模型桥接数据流

1. 桌面 Codex 只看到本地 provider：`http://127.0.0.1:17661/v1`。
2. 本地模型桥接服务收到 `/v1/responses`。
3. 插件把 Responses 请求转换为 Chat Completions 请求。
4. 插件把请求转发到当前选中的第三方 Base URL。
5. 如果有 Codex 会话 ID，插件会把它传给下游：请求体 `user = sessionContext.id`，并附加 `X-Codex-Session-Id`、`X-Codex-Session-Source`。

## 只在点击时打开的外部链接

| 入口 | URL | 触发 |
|---|---|---|
| Codex 文档 | `https://developers.openai.com/codex` | 帮助菜单点击 |
| Ai Open Tool | `https://aiopentool.com/` | 设置页维护区域点击支持入口；安装器报错弹窗选择支持入口 |
| DeepSeek 文档 | `https://api-docs.deepseek.com/api/create-chat-completion` | 模型接入页“打开文档” |
| 阿里 DashScope 文档 | `https://help.aliyun.com/zh/model-studio/compatibility-of-openai-with-dashscope` | 模型接入页“打开文档” |
| 智谱文档 | `https://docs.bigmodel.cn/cn/guide/develop/openai/introduction` | 模型接入页“打开文档” |
| 智谱 API Key 页面 | `https://open.bigmodel.cn/usercenter/apikeys` | 模型接入页“申请/打开 Key” |

## 不算运行时后台拉取的内容

- `package-lock.json` 里的 npm 依赖下载地址已改为 `https://raw.githubusercontent.com/chengyou888/-/main/npm-tarballs/...`，对应离线包来自 `https://github.com/chengyou888/-`；这是安装依赖用的，不是插件运行时后台拉取。
- `README.md`、`docs/`、`tweaks/AGENTS.md` 里的 URL 多数是文档示例或说明，不代表运行时一定访问。
- `store/index.json` 是仓库里的商店索引源文件；真正运行时默认读取的是 `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/index.json`。
- Codex 桌面应用本身的登录、同步、更新、遥测等网络访问不在本文统计范围内。

## 关闭或收敛建议

- 关闭 Codex++ 自动更新：在插件配置里关闭 `Automatically refresh Codex++`；对应 `config.json` 的 `codexPlusPlus.autoUpdate = false`。
- 关闭整个插件：在插件配置里关闭总开关；watcher 服务检测到 `enabled=false` 后会跳过 update 和 repair。
- 固定自更新源：把 `updateChannel` 设为 `custom`，并固定 `updateRepo/updateRef` 到自己的私有仓库和 tag/commit。
- 固定插件商店源：默认已指向 `https://raw.githubusercontent.com/chengyou888/-/main/codex-plusplus-store/index.json`；也可设置环境变量 `CODEX_PLUSPLUS_STORE_INDEX_URL` 指向自己的内网 JSON。
- 完全禁止远程商店：保留本地 tweaks 目录，屏蔽/移除 `fetchTweakStoreRegistry()` 调用或让 `CODEX_PLUSPLUS_STORE_INDEX_URL` 指向不可变内网空索引。
- 第三方模型 API：只有配置 API Key 并选择对应 provider 才会走外部模型服务；不需要时切回 Codex 原生模型。
